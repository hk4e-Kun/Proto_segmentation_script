{
  "target_files": [
    "example.txt",
    "source_code.h",
    "data_file.proto"
  ],
  "rules_file": "rules.txt",
  "backup_files": true,
  "case_sensitive": true,
  "output_suffix": "_replaced",
  "output_extension": null,
  "max_workers": 8,
  "_comments": {
    "target_files": "需要处理的目标文件列表",
    "rules_file": "替换规则文件的路径，格式为：原字符串 替换字符串",
    "backup_files": "是否备份原文件（生成.backup后缀的备份文件）",
    "case_sensitive": "是否区分大小写进行替换",
    "output_suffix": "输出文件的后缀名，会加在原文件名后面，例如：source_code.h -> source_code_replaced.h",
    "output_extension": "输出文件的扩展名：null=使用原文件扩展名，txt=统一使用.txt扩展名，cpp=统一使用.cpp扩展名",
    "max_workers": "最大工作线程数，建议设置为CPU核心数的1-2倍，默认为4"
  }
}